import { PerformanceMyCard } from '@/store/common';

export default function () {
    describe('AddMyCard', () => {
        it('測試新增 MyCard 點數', () => {
            expect(true).toBe(true);
        });
    });
}